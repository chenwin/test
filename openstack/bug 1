from distutils import version
from heat.common import exception

version.StrictVersion()


            raise InvalidArgForVersion(arg=self.READ_ONLY,
                                       min_version=READ_ONLY_MIN_API_VERSION)
