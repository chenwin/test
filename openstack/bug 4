https://bugs.launchpad.net/heat/+bug/1434427

Docker 1.2.0 and docker-py 0.6.0 support the device parameter when
starting containers(docker run --device[]).

Using --device you can add a host device to the container.(e.g. --device=/dev/sdc:/dev/xvdc)
Note:":" is used to split the full device path.
Reference:
https://github.com/docker/docker-py/blob/master/docker/utils/utils.py#L259 (parse_devices)

Closes-bug: #1434427

https://github.com/docker/docker-py/blob/master/docs/change_log.md
https://docs.docker.com/reference/api/docker_remote_api/
https://docs.docker.com/reference/commandline/cli/

DEVICE_REVERSED_CHAR = ':'

    def _get_mapping_devices(self, devices):
        actual_devices = []
        for device in devices:
            if DEVICE_REVERSED_CHAR in device[self.PATH_ON_HOST] \
                    or DEVICE_REVERSED_CHAR in device[self.PATH_IN_CONTAINER]:
                detail_reason = '%s is reserved characters in %s or %s' % \
                                (DEVICE_REVERSED_CHAR,
                                 self.PATH_ON_HOST, self.PATH_IN_CONTAINER)
                raise exception.Invalid(reason=detail_reason)

            actual_devices.append(device[self.PATH_ON_HOST] +
                                  DEVICE_REVERSED_CHAR +
                                  device[self.PATH_IN_CONTAINER] +
                                  DEVICE_REVERSED_CHAR +
                                  device[self.PERMISSIONS])
        return actual_devices


